#include "tree.h"
#include <stdio.h>

#include "test_tree.h"

int main()
{
    // struct BinTree tree;

    // int ret = initTree (&tree);
    // printf ("----------- ret = %d\n", ret);
    // // printf ("%p %d %d", tree.array_nodes, tree.used, tree.capacity);
    
    // // ret = addInTree (&tree, 10);

    // for (int i = 0; i < 11; i++){
    //     ret = addInTree (&tree, 10 + i);
    //     printf ("----------- ret add = %d\n", ret);
    // }

    // forEachTree (&tree, printData, NULL);
    // ret = delTree (&tree);
    // printf ("----------- ret del = %d\n", ret);
    runTest();
}

